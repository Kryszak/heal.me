spring:
  datasource:
    url: jdbc:h2:mem:healme
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  liquibase:
    contexts: it
    change-log: classpath:db/test.changelog.xml

logging:
  level:
    net.kryszak.healme: DEBUG