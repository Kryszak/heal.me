spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/healme
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    contexts: local

logging:
  level:
    net.kryszak.healme: DEBUG